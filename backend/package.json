{
  "name": "vehicle-report-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "worker": "node worker/index.js",
    "services:up": "node server.js & node worker/index.js",
    "docker:up": "docker compose -f docker-compose.yml up -d",
    "docker:down": "docker compose -f docker-compose.yml down"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bullmq": "^4.0.0",
    "ioredis": "^5.3.2"
  }
}
