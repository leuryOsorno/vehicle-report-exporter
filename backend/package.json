{
  "name": "vehicle-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "docker:up": "docker compose -f ../docker-compose.yml up backend redis",
    "docker:down": "docker compose -f ../docker-compose.yml down"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ioredis": "^5.3.1",
    "bullmq": "^4.5.1"
  }
}
